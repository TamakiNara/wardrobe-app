# 着物の形・補助情報メモ

## 現状

### 種類

- `kimono` / `yukata` / `japanese_accessory` / `other` を扱う

### 画面表示

- 着物は `subcategory` 主導で扱い、shape は画面上で表示しない
- `japanese_accessory` は和装小物として扱う

## 要再判断

- 着物本体と和装小物を将来的にカテゴリ分離するかは今回扱わない
- 着物内で shape を復活させる必要があるかは利用実態を見て再判断する
