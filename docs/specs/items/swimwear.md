# 水着の形・補助情報メモ

## 現状

### 種類

- `swimwear` / `rashguard` / `other` を扱う

### 画面表示

- 水着は `subcategory` 主導で扱い、shape は画面上で表示しない
- 以前の「種類任意・shape 必須」に見える状態は避け、種類を先に選ぶ流れに寄せる

## 要再判断

- 水着の上下別や競泳用など、種類の粒度を増やすかは今回扱わない
- 将来 spec を増やす場合も、まずは `subcategory` で表せないかを先に判断する
