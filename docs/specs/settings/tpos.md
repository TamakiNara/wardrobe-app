# TPO 任意追加仕様

## 目的

本仕様は、Wardrobe App における **TPO（利用シーン）をユーザーごとの管理対象として扱うための仕様** を定義する。  
現状は `仕事 / 休日 / フォーマル` を前提にした固定的な運用をしているが、今後はユーザーごとに必要な TPO を追加・無効化・並び替えできるようにし、item / outfit / wear log の各機能で一貫して利用できるようにする。

---

## 概要

TPO 任意追加は、設定配下で管理する **ユーザー単位の TPO マスタ機能** とする。  
TPO は自由入力で都度保存するのではなく、**あらかじめ登録済みの TPO から選択する方式** を前提とする。  
これにより、表記ゆれや重複登録を抑えつつ、一覧・絞り込み・将来拡張で扱いやすい状態を保つ。

---

## 背景

現在のアプリでは、TPO は実質的に固定値に近い扱いで運用している。  
一方で、実際の利用シーンはユーザーごとに差があり、以下のような要望に対応しにくい。

- `通院`
- `旅行`
- `推し活`
- `雨の日`
- `在宅勤務`
- `セミフォーマル`

TPO をユーザーごとに管理可能にすることで、既存の `仕事 / 休日 / フォーマル` をベースにしつつ、必要な利用シーンを追加できるようにする。  
ただし、TPO はタグのような完全自由分類ではなく、**選択肢として制御されたマスタ** として扱う。

---

## 位置づけ

- 設定系機能
- ユーザー単位マスタ
- item / outfit / wear log から参照される共通選択肢
- タグ機能の代替ではない
- 将来の絞り込み・集計・候補提案に使いやすい基盤

---

## current / planned / 要再判断

### current

- TPO は複数選択の概念として、item / outfit / wear log で使う前提がある
- プリセットとして `仕事 / 休日 / フォーマル` を基本に考えている
- settings 配下で管理する方向性は合意済み

### planned

- `user_tpos` によるユーザー単位 TPO マスタ
- プリセット TPO の初期投入
- ユーザー追加 TPO
- TPO の無効化
- TPO の並び替え
- `/settings/tpos` の管理画面
- `GET /api/settings/tpos`
- `POST /api/settings/tpos`
- `PATCH /api/settings/tpos/{id}`

### 要再判断

- 既存の `items.tpos` / `outfits.tpos` / wear log 側の保存値を、将来 `name` ベースのまま維持するか、TPO ID 参照へ寄せるか
- プリセット TPO の名称変更を本当に不可で固定するか
- 並び替え UI を上下ボタンにするか、ドラッグにするか
- 無効化した TPO を詳細画面や一覧フィルタでどう表示するか
- wear log で TPO をどの粒度まで使うか

---

## 基本方針

### 1. TPO はユーザーごとのマスタで持つ

TPO は全ユーザー共通マスタではなく、**ユーザー単位の管理データ** とする。  
理由は、必要な利用シーンがユーザーごとに異なるためである。  
ただし、初期状態で何も選べないと使いにくいため、プリセット TPO を各ユーザーへ初期投入する。

### 2. 自由入力ではなく登録済み TPO から選ぶ

item / outfit / wear log 側では、TPO を自由入力で都度保存しない。  
**登録済み TPO の選択式** とし、表記ゆれ・重複・ typo を防ぐ。

### 3. 削除より無効化を優先する

TPO は既存データとの整合のため、物理削除より **無効化** を優先する。  
無効化した TPO は、新規入力では選択肢から外すが、既存データでは保持・表示できる状態を前提とする。

### 4. 並び順はユーザーが管理できる

プリセット TPO とユーザー追加 TPO を分けて固定順にするのではなく、**同じ並び替え対象** として扱う。  
表示順は `sort_order asc` を正本とする。

---

## TPO の役割

TPO は「その item / outfit / wear log がどの利用シーンに向くか、または使われたか」を示す補助分類とする。

### item

- そのアイテムが向いている利用シーン
- 例: `仕事`, `休日`, `通院`

### outfit

- そのコーディネートが向いている利用シーン
- 例: `フォーマル`, `旅行`

### wear log

- 実際にその日にどの利用シーンで使ったか
- 将来的に item / outfit と異なる文脈を持つ可能性はあるが、当面は同じ TPO 選択肢を使う

---

## プリセット TPO

### 初期プリセット候補

- `仕事`
- `休日`
- `フォーマル` 

### 扱い

プリセット TPO は、各ユーザーに初期投入される標準選択肢とする。

#### 属性

- `is_preset = true`
- 削除不可
- 無効化可
- 名称変更は基本不可寄りとする 

### 理由

プリセットの名称変更を許可すると、画面上は柔軟だが、仕様共有やサポート時に「同じ意味の TPO が人ごとに別名になる」問題が起きやすい。  
一方で、不要な場合は無効化できれば十分なケースが多いため、初期方針としては **名称変更不可・無効化可** を推奨する。

---

## ユーザー追加 TPO

ユーザーは、自分に必要な TPO を追加登録できる。

### 追加条件

- 同一ユーザー内で重複不可
- 前後空白除去
- 空文字不可
- 文字数上限あり 

### 想定例

- `通院`
- `旅行`
- `在宅勤務`
- `推し活`
- `雨の日`

### 補足

ユーザー追加 TPO も、プリセットと同様に無効化対象とする。  
ただし、プリセットと異なり、名称は作成者自身が決めるため、更新可否は将来拡張余地がある。

---

## 削除と無効化

### 方針

- 削除より無効化を優先する
- 非 active TPO は新規選択肢に出さない
- 既存データでは保持し、表示できるようにする
- プリセット TPO も追加 TPO も無効化対象に含める 

### 推奨運用

#### プリセット TPO

- 物理削除しない
- 無効化のみ許可

#### ユーザー追加 TPO

- MVP では物理削除を持たず、無効化で対応する
- 将来、本当に未使用であれば削除可能にする案はありうるが、初期版では不要

### 理由

TPO は item / outfit / wear log に紐づくため、削除時に参照整合や表示ルールが複雑になる。  
そのため、まずは **無効化で管理する方が安全** である。

---

## 並び順

### 方針

- TPO はユーザーが並び順変更可
- プリセット TPO も追加 TPO も同じ並び替え対象に含める
- 管理用に `sort_order` を持つ
- 一覧表示は `sort_order asc` を正本とする 

### UI 上の扱い

初期版では、複雑な drag and drop よりも、上下移動ボタンの方が実装・テスト・保守の観点で扱いやすい。  
ただし、この点は UI 方針として `要再判断` に残してよい。

---

## データ構造案

### `user_tpos`

| column | type | description |
| --- | --- | --- |
| id | bigint | 主キー |
| user_id | bigint | 所有ユーザーID |
| name | string | TPO 名 |
| sort_order | unsigned int | 表示順 |
| is_active | boolean | 有効フラグ |
| is_preset | boolean | プリセット由来フラグ |
| created_at | timestamp | 作成日時 |
| updated_at | timestamp | 更新日時 |

### 制約案

- `unique(user_id, normalized_name)` 相当の重複防止
- `sort_order` はユーザー内で一意に近い運用を想定
- `user_id` は `users.id` を参照

### 補足

重複判定では、少なくとも前後空白除去と大文字小文字・全半角揺れを吸収できる余地を残す。  
正式な正規化ルールは実装時に別途定義してよい。

---

## API 案

### `GET /api/settings/tpos`

#### 目的

- 現在のユーザーが持つ TPO 一覧取得
- settings 管理画面表示
- item / outfit / wear log の選択肢取得

#### response 例

```json
{
  "tpos": [
    {
      "id": 1,
      "name": "仕事",
      "sortOrder": 1,
      "isActive": true,
      "isPreset": true
    },
    {
      "id": 2,
      "name": "休日",
      "sortOrder": 2,
      "isActive": true,
      "isPreset": true
    },
    {
      "id": 3,
      "name": "通院",
      "sortOrder": 4,
      "isActive": false,
      "isPreset": false
    }
  ]
}
```

### `POST /api/settings/tpos`

#### 目的

- ユーザー追加 TPO の新規作成

#### request 例

```json
{
  "name": "通院"
}
```

#### 方針

- 作成時の `sort_order` は末尾自動採番
- `is_active = true`
- `is_preset = false`

### `PATCH /api/settings/tpos/{id}`

#### 目的

- 名称更新
- 有効 / 無効切替
- 並び順変更

#### request 例

```json
{
  "name": "旅行",
  "isActive": true,
  "sortOrder": 5
}
```

#### 補足

初期版では「名称更新」と「並び順変更」を同一 endpoint で扱ってもよい。  
ただし、並び順変更を複数件一括更新にしたくなった場合は、将来 `reorder` 専用 API を切る余地がある。

---

## settings 画面案

### 画面配置

- 設定配下に独立画面を置く
- 例: `/settings/tpos` 

### 役割

- 一覧確認
- 追加
- 無効化
- 並び替え 

### 画面で見せたい情報

- TPO 名
- プリセットかどうか
- 有効 / 無効状態
- 並び順
- 操作導線

### 初期版の操作案

- 新規追加
- 有効 / 無効切替
- 上へ / 下へ移動
- 名称編集（追加 TPO のみ）
- プリセットは名称編集不可

---

## item / outfit / wear log との関係

### 基本方針

TPO マスタは settings 側に置くが、**利用先は item / outfit / wear log の共通選択肢** とする。  
したがって、設定画面だけで閉じる機能ではなく、将来の入力 UI・絞り込み UI と接続する前提で設計する。

### item

- 登録済み TPO から複数選択
- 非 active TPO は新規選択肢に出さない
- ただし、既存 item がすでに持っている非 active TPO は表示・保持可能にする

### outfit

- item と同様
- 既存 outfit に紐づいている非 active TPO は表示・保持可能にする

### wear log

- 現時点では将来利用前提
- item / outfit と同じ選択肢を使う方向
- ただし、wear log で TPO をどこまで必須にするかは未確定

---

## 既存保存構造との関係

これは現時点の重要な保留論点である。

### 現時点の課題

既存の item / outfit / wear log は、TPO を配列値として扱っている前提が強い。  
ここに `user_tpos` を導入する場合、次のどちらに寄せるかを決める必要がある。

### 案A: 当面は name ベース保存を維持する

- settings 側で選択肢だけ `user_tpos` を正本にする
- item / outfit / wear log の保存値は従来どおり文字列配列
- UI 側では `user_tpos.name` を選択肢として使う

#### メリット

- 既存 DB / API への影響が軽い
- 移行コストが小さい

#### デメリット

- 将来の名称変更や厳密参照に弱い

### 案B: 将来は TPO ID 参照へ寄せる

- 入力・保存ともに ID ベースへ移行する
- 表示時に `user_tpos` を join / map する

#### メリット

- 名称変更・無効化・整合性管理に強い
- 将来の集計や絞り込みで扱いやすい

#### デメリット

- 既存 API / DB / UI / データ移行の影響が大きい

### 推奨

現段階では **案A を当面の第一候補** とし、  
仕様書では「選択肢の正本は `user_tpos`、保存構造の全面 ID 化は将来検討」と分けておくのが現実的である。

---

## バリデーション方針

### 追加

- `name` 必須
- 前後空白除去後に空文字不可
- 文字数上限あり
- 同一ユーザー内で重複不可
- 無効化済み名称との重複も、原則不可寄りで扱う

### 更新

- 対象は自分の TPO のみ
- プリセット TPO の名称変更は不可寄り
- `is_active` は boolean
- `sort_order` は 1 以上の整数

---

## テスト観点

### 一覧

- 自分の TPO のみ返る
- `sort_order asc` で返る
- プリセットと追加 TPO が混在して返る
- 無効化済みも管理画面用途では返せる

### 追加

- TPO を追加できる
- 同一ユーザー内重複はエラー
- 前後空白除去後の空文字はエラー
- 作成時は末尾 `sort_order` になる

### 更新

- 無効化できる
- 再有効化できる
- 並び順変更できる
- 他人データは更新不可
- プリセット名称変更不可ルールを入れる場合はそのテストが必要

### 利用先

- item / outfit / wear log の選択肢として active TPO のみ出せる
- 既存データに含まれる inactive TPO は表示・保持できる

---

## 実装時の注意

- TPO をタグのような自由入力に戻さない
- settings 側だけで閉じず、利用先の表示責務まで意識する
- 無効化した TPO を既存データから一律削除しない
- 並び順ロジックを frontend に寄せすぎない
- 後から ID 参照へ移行できる余地を残す

---

## この仕様で固定する前提

1. TPO の選択肢正本を `user_tpos` にする
2. プリセット TPO は `仕事 / 休日 / フォーマル`
3. 削除より無効化を優先する
4. 並び順は `sort_order` で持つ
5. settings 配下に独立画面を置く
6. 既存保存構造の全面 ID 化は今回は確定しない

---

## 要再判断に残す論点

- item / outfit / wear log の保存値をいつ ID 化するか
- プリセット TPO の名称変更を完全不可で固定するか
- 並び替え UI の具体形式
- TPO 一括編集
- TPO 使用回数集計
- TPO ごとの分析表示

---

## 実装時に更新が想定される関連資料

将来、実装に進む段階では次の更新が想定される。

- `docs/specs/settings/tpos.md`（本ファイル）
- `docs/data/database.md`
- `docs/api/openapi.yaml`
- `docs/project/implementation-notes.md`
- item / outfit / wear log 側の各 spec

---

## 現時点のまとめ

TPO 任意追加は、ユーザーごとの利用シーンを柔軟に扱うための **設定系マスタ機能** として導入する。  
初期版では、

- プリセット TPO を持つ
- ユーザー追加 TPO を許可する
- 削除より無効化を優先する
- 並び順を管理できる
- item / outfit / wear log の共通選択肢として使う

という方針で整理する。  
一方で、既存保存構造をすぐ ID 化するかどうかは影響範囲が大きいため、今回は `要再判断` に残し、まずは選択肢管理の正本を整えることを優先する。
